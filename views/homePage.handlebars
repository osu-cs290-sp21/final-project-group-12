<main class="home-page">

   <div class="title-container">
         {{!-- <div class="specials-container"> --}}
            <h3>Specials</h3>
             {{#each specialItems}}
               {{> item}}
             {{/each}}

         </div>

    </div>

    <div class="title-container">
        <a href="/category/meats"><h3>Meats</h3></a>
            {{#each meatsItems}}
               {{> item}}
             {{/each}}
    </div>

    <div class="title-container">
        <a href="/category/veggies"><h3>Veggies</h3></a>
            {{#each veggiesItems}}
                {{> item}}
            {{/each}}
    </div>

    <div class="title-container">
        <a href="/category/drinks"><h3>Drinks</h3></a>
            {{#each drinksItems}}
                {{> item}}
            {{/each}}
    </div>

</main>
