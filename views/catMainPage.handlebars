<main class="category-main-page">
   <div class="title-container">
        <h3>{{key}}<i class="fas fa-heart"></i></h3>
        {{#each items}}
            {{> item}}
        {{/each}}
    </div>
</main>